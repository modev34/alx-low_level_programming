#include "main.h"

/**
* _islower - check if char is lowercase
* @c: is the variable that the function will check
* Return: 1 if char is lowercase, otherwise 0.
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
